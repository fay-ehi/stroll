/**
 * Stroll — Create Collection
 * app/(modals)/create-collection.tsx
 *
 * PRD §8.10 — Fields: Title, Description, Cover Image. After publishing,
 * the creator adds experiences from their own experience history only.
 * Sprint 4: placeholder only.
 */

import React from 'react';
import { PlaceholderScreen } from '@/components/placeholder/PlaceholderScreen';

export default function CreateCollectionModal() {
  return (
    <PlaceholderScreen
      title="Create Collection"
      description="Title, description, and cover image fields will appear here."
    />
  );
}
