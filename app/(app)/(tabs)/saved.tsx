/**
 * Stroll — Saved Tab
 * app/(app)/(tabs)/saved.tsx
 *
 * PRD §8.9 — "Want To Visit" list. The anchor for the core discovery loop
 * (Discover → Save → Visit → Create Experience → Others Discover).
 * Sprint 4 scope: placeholder only.
 */

import React from 'react';
import { PlaceholderScreen } from '@/components/placeholder/PlaceholderScreen';

export default function SavedScreen() {
  return (
    <PlaceholderScreen
      title="Saved"
      description="Your Want To Visit list — places you've saved to explore later."
    />
  );
}
