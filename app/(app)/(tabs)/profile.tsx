/**
 * Stroll — Profile Tab
 * app/(app)/(tabs)/profile.tsx
 *
 * PRD §8.11 — My Profile: avatar, display name, city, Experiences and
 * Collections tabs, follower/following counts, Edit Profile action.
 * Sprint 4 scope: placeholder only.
 */

import React from 'react';
import { PlaceholderScreen } from '@/components/placeholder/PlaceholderScreen';

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="Your profile, experiences, and collections will appear here."
    />
  );
}
