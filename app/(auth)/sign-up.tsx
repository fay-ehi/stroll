/**
 * Stroll — Sign Up
 * app/(auth)/sign-up.tsx
 *
 * PRD §8.1 — Fields: Username, Email, Password. Action: Create Account.
 * Sprint 4: placeholder only, no Supabase / auth logic.
 */

import React from 'react';
import { PlaceholderScreen } from '@/components/placeholder/PlaceholderScreen';

export default function SignUpScreen() {
  return (
    <PlaceholderScreen
      title="Sign Up"
      description="Username, email, and password fields will appear here."
    />
  );
}
