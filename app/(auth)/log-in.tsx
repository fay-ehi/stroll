/**
 * Stroll — Log In
 * app/(auth)/log-in.tsx
 *
 * PRD §8.1 — Fields: Email, Password. Action: Log In.
 * Sprint 4: placeholder only, no Supabase / auth logic.
 */

import React from 'react';
import { PlaceholderScreen } from '@/components/placeholder/PlaceholderScreen';

export default function LogInScreen() {
  return (
    <PlaceholderScreen
      title="Log In"
      description="Email and password fields will appear here."
    />
  );
}
